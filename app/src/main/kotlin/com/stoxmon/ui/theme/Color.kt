package com.stoxmon.ui.theme

import androidx.compose.ui.graphics.Color

// Background colors
val BackgroundDark = Color(0xFF1A0E2E)
val BackgroundCard = Color(0xFF2D1B4E)

// Primary colors
val PrimaryPurple = Color(0xFF8B5CF6)
val PrimaryPurpleLight = Color(0xFFA78BFA)

// Accent colors
val AccentPink = Color(0xFFE54188)
val AccentRed = Color(0xFFC41360)

// Text colors
val TextPrimary = Color(0xFFFFEBFF)
val TextSecondary = Color(0xFFFFEBFF).copy(alpha = 0.6f)
val TextTertiary = Color(0xFFFFEBFF).copy(alpha = 0.4f)

// Status colors
val PositiveGreen = Color(0xFF22C55E)
val NegativeRed = Color(0xFFEF4444)

// Component colors (from design)
val CardGradientStart = Color(0xFF550799)
val CardGradientEnd = Color(0xFF2D2D2D)

// Additional colors from palette
val ColorFFEBFF = Color(0xFFFFEBFF)
val Color2D2D2D = Color(0xFF2D2D2D)
